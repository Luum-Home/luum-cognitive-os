---
type: quality-synthesis
source: docs/09-Quality/legal/pre-public-readiness-checklist.md
provenance: "The single gate document blocking the repository's flip to public visibility — every item must reach done or accept-risk with rationale before public launch."
---

## What it is
The master pre-public-launch checklist: a Critical/High/Medium/Low severity-tiered gate covering git-history sanitization, client-project decoupling, licensing, test health, operator-data privacy, AI-attribution disclosure, provenance/clean-room audit, supply chain, onboarding, and ADR quality — each item requiring evidence (commit SHA, report path) before the repo can go public.

## Key mechanics
- Status legend: `done` (verified, evidence linked), `in-progress`, `pending`, `accept-risk` (deliberate gap with rationale), `n/a`.
- **Critical (hard blockers), all `done`**: C1 git history sanitized via `cos history sanitize --execute` (ADR-218, 12 env vars, tombstone branch, force-pushed, tag reattached); C2 client-project coupling removed across Tiers 1-4 (hooks, skills, docs, test fixtures) with `audit-consumer-dependence.sh` returning 0 matches; C3 license transition (Apache 2.0 → FSL-1.1-MIT) documented via `license-faq.md`, matching `LICENSE`/`NOTICE`, and a README badge link; C4 tests green on main (portability lane 165/165, audit lane 3325 passed/0 failed) — but the Go-suite checkboxes (`go test ./...`, `gofmt -l`, `go vet`, CI-on-fresh-branch) remain **unchecked** even though the section is marked `done`, explained as "verified manually, CI pipeline still being wired"; C5 Engram store privacy audit complete with remediation pending operator decision, yet also marked `done`.
- **High, all `done`**: H1 aspirational-vs-real feature labelling (`aspirational_audit.py`: 1019 components, REAL 317/DORMANT 182/ASPIRATIONAL 36/METADATA 61); H2 AI-attribution policy published in `CONTRIBUTING.md` explaining the no-`Co-Authored-By` rule; H3 provenance/clean-room audit (20-tool provenance table, 15 ADRs spot-checked, 0 HIGH findings) plus every UNKNOWN-license SBOM entry individually classified (186 raw rows → 0 BLOCKED, 1 REVIEW).
- **Medium, all `done`**: M1 supply chain (SBOM via syft 1.44.0, 0 BLOCKED/15 REVIEW/0 UNKNOWN/186 OK licenses, per-language digest-pinning verdict, signed-release runbook — signing itself deferred as a release-time step, not a blocker); M2 onboarding walkthrough (fresh clone to first skill invocation, live-measured ~7 min); M3 ADR sweep (21 ADRs reviewed, 1 CRITICAL fixed, 4 MEDIUM resolved, ~16 LOW logged — see `m3-adr-sweep-report.md`); M4 sanitize-tombstone smoke test (0 hits across HEAD/tombstones/reflog).
- **Low, all `done`**: L1 operator personal-data leakage check (`operator-data-scan.md`); L2 worktree/temp-path leakage check (`operator-paths-scrub-report.md`).
- Sign-off table (Repository operator / Legal / Security / Comms-launch lead — name/date/signature) is present but **entirely blank** — no reviewer has signed off despite every tracked item showing `done`.
- Usage protocol: one PR per item, inline status updates, evidence linked next to each item, `accept-risk` requires a written rationale paragraph, final sign-off only after the table is filled.

## Relations & where used
Directly evidenced by and cross-linked to: `docs/09-Quality/legal/m3-adr-sweep-report.md` (M3), `docs/09-Quality/legal/operator-data-scan.md` (L1), `docs/09-Quality/legal/operator-paths-scrub-report.md` (L2), `docs/09-Quality/legal/h3-unknown-license-resolution.md` (H3), `docs/09-Quality/legal/license-faq.md` (C3), `docs/09-Quality/legal/h1-feature-status-audit.md` (H1), `docs/09-Quality/security/supply-chain.md` and `release-signing.md` (M1), `docs/05-Methodology/onboarding/walkthrough.md` (M2), `docs/04-Concepts/architecture/provenance.md` (H3), `docs/04-Concepts/root/safety-mesh.md`, ADR-218/220/226, `manifests/history-sanitization.yaml`, `scripts/audit-consumer-dependence.sh`.

## Status / caveats
This is a dated, point-in-time snapshot (evidence dated 2026-05-07/08) presented as fully `done` at every tier, but contains internal contradictions worth flagging: (1) C4 is marked `done` while three of its six sub-checkboxes are unchecked (`[ ]`) — Go test suite, `gofmt`/`go vet`, and CI-on-fresh-branch are described as "verified manually" with CI "still being wired," which is a done/checkbox mismatch; (2) C5 is marked `done` while all four of its sub-checkboxes are unchecked, with remediation explicitly described as "operator decision pending"; (3) the final sign-off table is blank across all four reviewer roles despite the document's own rule that "public-visibility flip requires" that table be filled — meaning the gate's own closing condition is not met even though every section above it reads `done`. These are source-document inconsistencies, not corrected here.
