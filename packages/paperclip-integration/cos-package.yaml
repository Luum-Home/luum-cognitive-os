name: "@luum/paperclip-integration"
version: "1.0.0"
description: "Paperclip governance dashboard integration — session metrics sync on Stop"
authors: ["Cognitive OS Team"]
license: "Apache-2.0"
keywords: ["paperclip", "governance", "dashboard", "metrics", "sync"]
cos_version: ">=0.1.0"

provides:
  - hook

exports:
  - source: "hooks/paperclip-sync.sh"
    type: hook
    description: "Sync session metrics to Paperclip dashboard on session end"
    hook_event: Stop
