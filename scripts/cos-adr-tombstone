#!/usr/bin/env bash
# SCOPE: both
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="${COGNITIVE_OS_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-${CODEX_PROJECT_DIR:-$(pwd)}}}"
python3 "$SCRIPT_DIR/adr_tombstone.py" --project-dir "$PROJECT_DIR" "$@"
