#!/usr/bin/env bash
# SCOPE: os-only
# PURPOSE: Probe service-control-plane provider auth without reading credential stores.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/cos_auth_probe.py" "$@"

