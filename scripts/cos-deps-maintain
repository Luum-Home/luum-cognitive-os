#!/usr/bin/env bash
# SCOPE: os-only
# Advisory dependency maintenance entry point for install/update/git-hook flows.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
exec python3 "$REPO_ROOT/lib/dependency_maintenance.py" "$@"
