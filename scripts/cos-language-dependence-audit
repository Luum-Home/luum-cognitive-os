#!/usr/bin/env bash
# SCOPE: os-only
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHONPATH="$ROOT${PYTHONPATH:+:$PYTHONPATH}" python3 -m cos_lib.language_dependence_audit --root "$ROOT" "$@"
