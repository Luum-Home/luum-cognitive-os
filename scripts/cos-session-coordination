#!/usr/bin/env bash
# SCOPE: os-only
set -euo pipefail

PROJECT_DIR="${COGNITIVE_OS_PROJECT_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
python3 "$PROJECT_DIR/scripts/cos_session_coordination.py" --project-dir "$PROJECT_DIR" "$@"
